#include <stdio.h>
#include <stdlib.h>

int main()

{
	float x, y;
    printf("x=");
	scanf("%f" ,&x);
	y = x * x;
	printf ("y=%f", y);
	
    printf("\nx=");
	scanf("%f" ,&x);
	y = x * x;
	printf ("y=%f", y);
system("pause");
    return 0;
}
